import UnityEngine

class CubeCharacters (MonoBehaviour): 

	public cubeName = "Cube"
	public characters as (single)
	// must set up characters in the inspector for all cubes
	// index : 1=move 2=jump 3=push 4=activate

	def Start ():
		pass

